const themeClasses=["theme--default","theme--dark"],splashNode=document.getElementById("splash-text"),splashTarget="Benjamin Mastripolito";var themeIdx=0,splashTextChar=0;function toggleTheme(){themeIdx=(themeIdx+1)%themeClasses.length,document.body.className=themeClasses[themeIdx],localStorage.setItem("theme",themeIdx.toString())}function writeSplashText(){splashTextChar+=1,splashTextChar<=splashTarget.length?(splashNode.innerHTML=`${splashTarget.slice(0,splashTextChar)}|`,setTimeout(writeSplashText,15-Math.random()*10+Math.random()*20)):splashNode.innerHTML=splashTarget}localStorage.getItem("theme")&&(themeIdx=parseInt(localStorage.getItem("theme")),isNaN(themeIdx)&&(themeIdx=0,localStorage.removeItem("theme"))),themeIdx=(themeIdx+1)%themeClasses.length,toggleTheme(),splashNode&&writeSplashText()